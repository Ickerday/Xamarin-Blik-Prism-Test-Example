<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="BlikPrismApp.Views.BlikConfirmationPage">
    <StackLayout Margin="5" 
                 Spacing="20">
        <StackLayout Margin="10" Spacing="10">

            <Label Text="{Binding Recipient}" 
                   FontSize="Large" />

            <Label Text="{Binding OperationName}"
                   FontSize="Large" />

            <Label Text="{Binding Amount}"
                   FontSize="Large" />

        </StackLayout>

        <Label Text="Confirm payment:" 
               FontSize="Small" />

        <Entry Placeholder="PIN"
               Text="{Binding PinCode, Mode=OneWayToSource}" />

        <Button Text="Confirm" 
                Command="{Binding ConfirmBlikCode}" />

    </StackLayout>
</ContentPage>